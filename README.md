# openanim (engine)

Deterministic, local-first compilation/rendering engine for code-based animation.

This repository is the OSS engine only (IR, graph, diffing, providers, sandbox, caching, CLI).
The hosted SaaS lives in a separate repo and consumes this engine as a versioned package.